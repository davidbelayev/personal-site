---
import BaseHead from '../../../components/BaseHead.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';

const pageTitle = 'Internal Host Compromise';
const pdfPath = '/assets/pdfs/cyberlabs-test.pdf';
---

<html lang="en">
  <head>
    <BaseHead title={pageTitle} description={pageTitle} />
    <style>
      main.report { max-width: 980px; margin: 0 auto; padding: var(--page-padding-vertical) var(--page-padding-horizontal); }
      .viewer-wrap { width: 100%; max-width: 100%; overflow: auto; }
      .viewer { width: 100%; max-width: 100%; display: block; height: calc(100vh - 240px); border: 1px solid rgba(var(--gray),8%); border-radius:8px; min-width: 0; }
      .actions { margin: 0.75rem 0; display:flex; gap:0.5rem; align-items:center; }
      .btn { background: transparent; border: 1px solid rgba(var(--gray),0.12); padding:0.5rem 0.75rem; border-radius:8px; text-decoration:none; color:inherit; }
      /* Prevent the report page from producing a horizontal page scroll when the PDF is wide */
      html, body { overflow-x: hidden; }
    </style>
  </head>
  <body>
    <Header />
    <main class="report" role="main">
      <header>
        <h1>{pageTitle}</h1>
      </header>

      <div class="actions">
        <a class="btn" href="/projects">← Back to Projects</a>
        <a class="btn" href={pdfPath} target="_blank" rel="noopener">Open PDF in new tab</a>
        <a class="btn" href={pdfPath} download>Download PDF</a>
      </div>

      <div class="viewer-wrap">
        <iframe class="viewer" src={pdfPath + '#zoom=page-width'}></iframe>
      </div>
    </main>
    <Footer />
  </body>
</html>
